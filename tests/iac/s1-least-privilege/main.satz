// IaC-roles gate: the S1 group model in one file, and Defender for Cloud's CSPM
// role in least-privilege mode. Neither can share an estate with `all-packs` —
// S1 and S2 define the same groups, the two CSPM role modes the same role.

estate iac_s1_least_privilege

params {
  customer_organization_id = "123456789012"
  customer_id              = "C0example"
  customer_domain          = "example.com"
  customer_shortname       = "acme"
  infra_project_name       = "acme-infra-001"
  svc_iac_account          = "svc-iac-001"
  billing_account_infra    = "012345-6789AB-CDEF01"
  mdc_workload_pool_id     = "11111111111111111111111111111111"
  mdc_mgmt_project_id      = "acme-mdc-001"
}

terraform {
  backend {
    local { path = "terraform.tfstate" }
  }
}

use "presets/security-group-models/s1-security-groups.satz"

use "presets/integrations/microsoft-defender-for-cloud.satz"
use "presets/integrations/microsoft-defender-for-cloud-cspm.satz"
use "presets/integrations/microsoft-defender-for-cloud-cspm-role-least-privilege.satz"
