<!-- generated by `satz doc-packs` from the pack file — edit the pack or the notes region, never the rest -->
# `integrations.microsoft_defender_for_cloud_cspm` — version 0.1

Source: `presets/integrations/microsoft-defender-for-cloud-cspm.satz`

## Purpose

Microsoft Defender for Cloud — the CSPM plan.

Used beside the foundation pack, gated on the licence:
use "presets/integrations/microsoft-defender-for-cloud-cspm.satz" when mdc_plan_cspm

The custom role this plan grants is NOT here: it depends on the access mode,
so it lives in its own fragment (`-cspm-role-default` or
`-cspm-role-least-privilege`) and exactly one of those is used. This file is
the part that is the same either way.

Everything is declared at top level with an explicit project, because a
fragment cannot nest into the project the foundation pack defines.

## Params

_None — the pack takes everything from the estate's params._

## Contributes

| resource type | labels |
|---|---|
| `google_iam_workload_identity_pool_provider` | `mdc_cspm` |
| `google_organization_iam_member` | `serviceAccount:microsoft-defender-cspm@{mdc_mgmt_project_id}.iam.gserviceaccount.com (grant map)` |
| `google_service_account` | `mdc_cspm` |
| `google_service_account_iam_member` | `service_account_id (grant map)`, `principalSet://iam.googleapis.com/projects/${google_project.mdc_mgmt.number}/locations/global/workloadIdentityPools/{mdc_workload_pool_id}/* (grant map)` |

## Claims

_None — this pack proves no control by itself._

## Notes

<!-- notes:start — hand-written, kept across `satz doc-packs` -->

_No notes yet._

<!-- notes:end -->
