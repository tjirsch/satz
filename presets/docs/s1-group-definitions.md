<!-- generated by `satz doc-packs` from the pack file — edit the pack or the notes region, never the rest -->
# `s1_group_definitions` — version 1.3

Source: `presets/security-group-models/s1-group-definitions.satz`

## Purpose

The five S1 admin groups, defined but not populated.

To adopt groups that already exist in the tenant, run
satz adopt <estate> --only google_cloud_identity_group --execute
Each group is looked up by email over the Cloud Identity API and the verified
ids are written into the estate as "import-id". That is why no group ids are
hard-coded here — they are tenant-specific and do not belong in a shared preset.

Memberships are deliberately NOT part of this preset: presets define groups,
humans grant membership. Estates that need managed memberships declare them
as estate-level resources.

## Use it

```
params {
  // the pack's own defaults — copy only the lines you change
  gcp_organization_admins_name = "gcp-organization-admins"
  gcp_project_admins_name = "gcp-project-admins"
  gcp_security_admins_name = "gcp-security-admins"
  gcp_security_viewers_name = "gcp-security-viewers"
  gcp_billing_admins_name = "gcp-billing-admins"
}

google_cloud_identity_group { use "presets/security-group-models/s1-group-definitions.satz" }
```

**Needs from outside the pack:**

- `infra_project_name` — declared by `estate_core`; use one of them alongside, or declare it in the estate.
- `svc_iac_account` — declared by `estate_core`; use one of them alongside, or declare it in the estate.

## Params

| param | default |
|---|---|
| `gcp_organization_admins_name` | `"gcp-organization-admins"` |
| `gcp_project_admins_name` | `"gcp-project-admins"` |
| `gcp_security_admins_name` | `"gcp-security-admins"` |
| `gcp_security_viewers_name` | `"gcp-security-viewers"` |
| `gcp_billing_admins_name` | `"gcp-billing-admins"` |

## Questions

What to ask before this pack is configured, and what changing the answer costs.

| asks about | prompt | reversal | blast | recommends |
|---|---|---|---|---|
| `gcp_organization_admins_name` | The organization admins group's name (the local part; the customer domain is added) | recreate | low | — |
| `gcp_project_admins_name` | The project admins group's name (the local part; the customer domain is added) | recreate | low | — |
| `gcp_security_admins_name` | The security admins group's name (the local part; the customer domain is added) | recreate | low | — |
| `gcp_security_viewers_name` | The security viewers group's name (the local part; the customer domain is added) | recreate | low | — |
| `gcp_billing_admins_name` | The billing admins group's name (the local part; the customer domain is added) | recreate | low | — |

**gcp_organization_admins_name** — The address is the group's identity: renaming it later means a new group, every member moved by hand and every role re-granted. This group holds the organisation's highest roles.


**gcp_project_admins_name** — The address is the group's identity: renaming it later means a new group, every member moved by hand and every role re-granted.


**gcp_security_admins_name** — The address is the group's identity: renaming it later means a new group, every member moved by hand and every role re-granted.


**gcp_security_viewers_name** — The address is the group's identity: renaming it later means a new group, every member moved by hand and every role re-granted.


**gcp_billing_admins_name** — The address is the group's identity: renaming it later means a new group, every member moved by hand and every role re-granted.


## Contributes

A bare list — keyed by the estate's resource map (`google_x { use "presets/security-group-models/s1-group-definitions.satz" }` or `use … as google_x`): `{gcp_organization_admins_name}`, `{gcp_project_admins_name}`, `{gcp_security_admins_name}`, `{gcp_security_viewers_name}`, `{gcp_billing_admins_name}`.

## Claims

_None — this pack proves no control by itself._

## History

| version | date | change |
|---|---|---|
| 1.3 | 2026-09-10 | five `question` blocks, one per group name, same reasoning. Nothing emitted changes |
| 1.2 | 2026-08-28 | group `lifecycle { ignore_changes = [initial_group_config] }` — an adopted group no longer plans as "must be replaced" |
| 1.1 | 2026-08-21 | ships NO human memberships — presets define groups, humans grant membership |
| 1.0 | 2026-08-20 | the five S1 admin groups |

The whole library's history: [the changelog](../README.md#changelog) in `presets/README.md`.

## Notes

<!-- notes:start — hand-written, kept across `satz doc-packs` -->

_No notes yet._

<!-- notes:end -->
