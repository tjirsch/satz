<!-- generated by `satz doc-packs` from the pack file — edit the pack or the notes region, never the rest -->
# `billing_account_permissions` — version 1.1

Source: `presets/billing-account-permissions.satz`

## Purpose

Billing-account IAM, split by audience: everyone in the domain may USE and
VIEW the billing account (link projects, read costs); the full
administration — and every other billing-account role — goes to ONE group,
named by a param so no customer forks the pack for it. The IaC service
account keeps `billing.admin`: it manages this account's budgets and
project links on every apply.

## Params

| param | default |
|---|---|
| `billing_admins_group` | `"gcp-billing-admins@{customer_domain}"` |

## Contributes

| resource type | labels |
|---|---|
| `google_billing_account_iam_member` | `billing_account_id (grant map)`, `serviceAccount:{svc_iac_account}@{infra_project_name}.iam.gserviceaccount.com (grant map)`, `domain:{customer_domain} (grant map)`, `group:{billing_admins_group} (grant map)` |

## Claims

_None — this pack proves no control by itself._

## Notes

<!-- notes:start — hand-written, kept across `satz doc-packs` -->

_No notes yet._

<!-- notes:end -->
