// GENERATED by scripts/build_dry_run_fragments.py from confidential-computing.satz — do not edit.
//
//   use "presets/cis-extensions/confidential-computing-dry-run.satz" when cis_confidential_computing_dry_run
//
// The dry-run twin of confidential-computing.satz. Google evaluates every rule below, writes a
// violation to the audit log for each action it WOULD have blocked, and blocks
// nothing — so the organisation's own numbers answer "will this break us?" before
// the control is enforced. Read them in Cloud Logging:
//
//   protoPayload.metadata."@type"="type.googleapis.com/google.cloud.audit.OrgPolicyViolationInfo"
//
// Promote by switching cis_confidential_computing_dry_run off
// and cis_confidential_computing on. Both on at once is refused:
// the estate would declare the same policy twice, once measuring and once blocking.
//
// This fragment carries NO claim. A dry run discharges no control while it runs, and
// `require` reports the control as unmet, which is the truth.

pack cis_extensions.confidential_computing_dry_run version "1.0"

google_org_policy_policy {
  "compute-managed-restrictNonConfidentialComputing" {
    name = "compute.managed.restrictNonConfidentialComputing"
    parent = "organizations/{customer_organization_id}"
    dry_run_spec {
      rules = [
        {
        enforce = "TRUE"
        },
      ]
    }
  }
}
