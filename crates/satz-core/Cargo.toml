[package]
name = "satz-core"
version = "0.1.0"
edition = "2021"
description = "Typed composition core for satz: fragments, lawful merge, provenance"
license = "MIT"
publish = false

[dependencies]
serde_yaml = "0.9"

[dev-dependencies]
proptest = "1"
