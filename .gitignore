.agent
.cursor
.claude
.terraform.lock.hcl
.terraform/
/organizations/*
/target
.env
*.secret
.DS_Store
*.log

# Session handover notes (local working docs, not for the public repo)
HANDOVER-*.md
# satz build artifacts (customer-side); upstream twins are built via `build-packs`
*.gen.yaml
*.gen.claims.yaml

# Private working state — never in the public repo (also rejected by the gate)
CLAUDE.local.md
*.local.md
.claude/
attestations.yaml
evidence/
# the rendered documentation site (built by scripts/build-site.py, published by CI)
_site/
__pycache__/
# provider schemas written by `satz update-schema` — per-checkout, tens of MB.
# Root-anchored so the trimmed CI fixture tests/schemas/ stays tracked.
/schemas/
# vendor Terraform handed over for an import: customer data, never committed
mdc-terraform.tf
