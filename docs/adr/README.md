# Architecture decision records

One file per decision that was **not obvious**, in [MADR](https://adr.github.io/madr/)
form: the context, the options weighed, what was chosen, and what it costs.

A decision belongs here when reversing it would be expensive, when a reader of the
code would otherwise ask "why on earth" — or when it was a genuine choice between
defensible alternatives. Most changes are none of those and need no record.

## Why they live here rather than in a commit message

A commit message explains one change to whoever reads that commit. An ADR answers
"why is it like this?" months later, when nobody is looking at the commit and the
alternative has started to look attractive again. Several decisions in this repository
were re-proposed after the reasoning was lost; that is the failure this directory
exists to prevent.

## Conventions

- `NNNN-kebab-title.md`, numbered in order, never renumbered. Take the next free
  number from the directory, not from memory: two records were both written as 0005
  on 2026-09-09, and a collision is fixed the day it is made, before anything links
  to the record.
- **Status** is `proposed`, `accepted`, `superseded by ADR-NNNN`, or `rejected`.
  A superseded record is not deleted — the reasoning that was right at the time is
  the thing worth keeping, and the successor should say what changed.
- Options are written with their real trade-offs, including the one that was chosen.
  A record where the alternatives are strawmen documents nothing.
- Nothing here names a customer, an organisation or a person: this repository is
  public, and the privacy gate treats these files like any other.

These pages are **not** published to <https://tjirsch.github.io/satz/>. The site's
navigation is a decided list of pages for people using satz; these are records for
people changing it. `scripts/build-site.py` only reads `docs/*.md`, so a record here
needs no entry in `SITE_DOCS` and implies no navigation decision.

## Records

| | what it decides | status |
|---|---|---|
| [0001](0001-mcp-identity-is-scoped-to-the-call.md) | the MCP server scopes the identity to each call, not the process | accepted; deriving it at `satz_open` superseded by 0030 |
| [0002](0002-superseded-org-policies-replace-by-construction.md) | superseded org policies change address so the plan is a replace | accepted |
| [0003](0003-evidence-is-data-the-audit-pack-is-not-satz-s-to-render.md) | evidence is data; the audit pack is the agent's to render | accepted |
| [0004](0004-the-verification-runner-is-a-pack-and-its-pipeline-is-inline.md) | the verification runner is a pack, and its pipeline is inline | accepted |
| [0005](0005-adopt-moves-a-renamed-block-rather-than-importing-it-again.md) | adopt moves a renamed block rather than importing it again | accepted |
| [0006](0006-an-answer-is-a-param-the-estate-binds.md) | an answer is a param the estate binds; nothing may be left unanswered | accepted |
| [0007](0007-the-map-is-a-pack-of-choices-and-the-estate-carries-the-lines.md) | the estate map is a pack of choices; the estate carries the `use` lines | accepted; the CIS baseline as no choice superseded by 0028, "the choices and nothing else" by 0031 |
| [0008](0008-the-site-renders-markdown-with-githubs-parser.md) | the site renders markdown with GitHub's own parser (cmark-gfm) | accepted |
| [0009](0009-iac-service-account-named-roles.md) | the IaC service account holds named roles, derived from the resource types; `update-prerequisites` writes them | accepted |
| [0010](0010-the-minor-version-marks-an-upgrade-that-brings-work.md) | the minor version marks an upgrade that brings estate work; everything else is a patch | accepted |
| [0011](0011-plan-and-apply-replace-a-policy-switching-to-reset.md) | `satz plan` and `satz apply` replace an org policy the state holds with rules and the estate declares reset | accepted |
| [0012](0012-the-admin-port-policy-ships-on-and-passes-the-private-ranges.md) | the admin-port policy ships on and passes the private ranges | accepted |
| [0013](0013-a-claim-asserts-what-its-witness-does.md) | a claim asserts what its witness DOES; an org policy that is switched off discharges nothing | accepted |
| [0014](0014-a-dry-run-is-a-generated-twin-and-claims-nothing.md) | a dry-run policy is a GENERATED twin of its enforcing fragment and carries no claim | accepted |
| [0015](0015-an-exemption-is-a-tag-and-the-control-stays-on.md) | an exemption is a tag binding; the control stays enforced and the exemption is reported beside it | accepted |
| [0016](0016-editor-support-is-a-tree-sitter-grammar-in-its-own-repository.md) | editor support is a tree-sitter grammar in its own repository; the Zed extension lives here and pins one commit of it | accepted |
| [0017](0017-the-formatter-keeps-the-authors-line-breaks.md) | `satz fmt` works on the token stream and keeps the author's line breaks; alignment is per run, meaning is proven by the canonical form | accepted |
| [0018](0018-editor-intelligence-comes-from-the-satz-binary.md) | `satz lsp` serves editors from satz's own parser, pipeline and schema; parse per change, pipeline per save; a pack is compiled through the estates that use it | accepted |
| [0019](0019-an-import-skips-what-the-platform-owns-and-says-so.md) | a live import skips what the platform owns — built-in sinks, service agents' grants, legacy bucket grants, default service accounts, deleted projects — as `skip:` patterns on the import-config row, reported per pattern | accepted |
| [0020](0020-a-discovered-estate-binds-the-library-s-vocabulary.md) | a discovered estate binds the day-0 vocabulary: derived facts without comment, inferred values with their rule beside them, what nothing states left out and reported; every bound literal referenced the library's way | accepted |
| [0021](0021-one-format-one-file-one-artefact.md) | a reporting command takes `--format` and `--out`, both required, and writes exactly one artefact at exactly one named path; `xlsx` is a format, `--report` and `--xlsx` are gone, and `update-prerequisites` and `prowler` keep the console | accepted |
| [0022](0022-the-licence-is-apache-2-0.md) | satz is licensed under Apache 2.0 alone — the express patent grant and the contribution terms in §5, with a `NOTICE` file that travels with redistribution | accepted |
| [0023](0023-one-command-for-every-prerequisite.md) | one command for every prerequisite a resource type implies — `update-prerequisites` writes the missing roles AND APIs into the estate, `merge-presets` runs it at the end of a pickup, and the compiler orders every resource after the service that enables its API | accepted |
| [0024](0024-the-pack-rules-live-in-satz.md) | the rules a pack must clear live in satz as `review-pack`, not in the app that renders them; a pack is folded into a synthesised estate to see what it emits, and the review says what adopting it costs | accepted |
| [0025](0025-the-pdf-is-typeset-in-the-binary.md) | `--format pdf` is typeset by Typst compiled into satz, with its fonts, instead of shelling out to pandoc and a LaTeX engine — accepted at three times the binary size, because a deliverable that needs two installed tools is not one | accepted |
| [0026](0026-a-command-declares-its-formats-once.md) | a command declares its formats once, as the value parser of `--format`, so the help lists exactly what it writes; a command that writes markdown writes pdf; `--out` may leave the extension off; global options are listed by `satz --help` alone | accepted |
| [0027](0027-what-windows-support-means.md) | Windows support is a tested x86_64 and ARM64 build with a PowerShell installer that works or refuses clearly — CI runs clippy and the tests on Windows, CRLF reads as LF, script actions and self-update are refused by name; no MSI, no signing yet, the smoke matrix stays on Linux | accepted; ARM64 superseded by 0029 |
| [0028](0028-a-pack-carries-its-own-resource-type.md) | a pack carries its own resource type and is `use`d bare, or is a bare list of labels the estate keys with a resource map — the shapes are not interchangeable and the wrong pairing is refused; the CIS baseline takes the first shape, joins the map as `use_cis_baseline`, and the CIS files move to `presets/cis/`, migrated by a `MOVED_PACKS` table the compile refuses against and `merge-presets` repoints from | accepted; the migration by `merge-presets` superseded by 0041 |
| [0029](0029-four-release-targets.md) | satz is released for four targets — Apple-silicon macOS, Linux on x86_64 and ARM64, Windows on x86_64; an Intel Mac or an ARM64 Windows machine builds from source, because the installers stop rather than fall back | accepted |
| [0030](0030-an-estate-that-names-no-identity-is-refused-and-mcp-derives-it-per-call.md) | an estate whose params cannot be read, or whose `deployment_mode` the compile refuses, names no identity and every command that would act as it refuses; `satz mcp` derives the identity per call, from the estate the call names or the open one, as it stands | accepted; supersedes 0001's derivation at `satz_open` |
| [0031](0031-the-map-offers-every-pack-and-the-graph-ships-with-the-presets.md) | the map carries one `offers` entry per pack — gate, phase, block, adoption order; edges between packs are derived from their param references and `ask_when` and declared on an entry only where the packs do not show them; `satz pack-graph` checks the library and writes `presets/pack-graph.json`, which ships with the presets rather than in the binary | accepted; supersedes 0007's "the choices and nothing else" |
| [0032](0032-one-pack-logic-reads-the-estate-against-the-graph.md) | one module (`src/packs.rs`) reads the estate against the shipped pack graph for the report, `add-pack`, `remove-pack`, the interview, `merge-presets` and the compile; gate values are read from the text; `requires`, `gate` and `data` edges are requirements, a `data` one met by the estate binding what the pack reads; lines go where the graph orders them | accepted; "until the gating migration runs" superseded by 0041 |
| [0033](0033-the-gating-migration-binds-what-deployed.md) | `merge-presets` gates every active line of a gated pack written without `when` and binds the gate `true` where the line deployed — a default included, a follower kept at its value, the tfvars lines of the bound gates the one part of the emission the proof lets move; two packs that exclude one another both deploying are refused | superseded by 0041 |
| [0034](0034-a-pack-names-the-command-to-run-once-it-is-on.md) | a pack names the ONE command to run once it is switched on (`notice`), shown when it goes on and warned at until the estate binds the notice's param `true`; `before = apply` refuses `transpile --apply` and `bootstrap`, `adopt --execute --import` binds the params of the notices that name it, and the param is emitted nowhere | accepted; the `before = apply` half superseded by [0042](0042-a-pack-declares-the-severity-of-what-it-says.md) |
| [0035](0035-an-answer-is-what-the-estate-binds.md) | the compile reports an unadopted pack from the estate's OWN binding of the gate, never from the library's default: a default in the map or in a pack is a proposal, so a day-0 skeleton with the map on compiles clean at `validation_level = "error"` and `satz packs` is where the proposals are read | accepted; 0006 applied to the pack-graph check |
| [0036](0036-plan-and-apply-enable-the-apis-the-estate-declares.md) | `satz plan` and `satz apply` enable every API the estate declares on the project the provider bills to and that is still off, as the estate's IaC service account, and stop before `tofu` with the filled-in `gcloud services enable` line where that is refused; `update-prerequisites` stays an estate edit and prints the same line | accepted |
| [0037](0037-a-finding-is-silenced-by-its-identity-in-three-tiers-and-never-dropped.md) | a finding is named by its `kind` + `subject` and silenced by that pair in three tiers — the estate's `config.toml` (a subject allowed, a `reason` mandatory), the operator's `~/.config/satz/satz.toml` (whole kinds) and the run (`--silence`, `SATZ_SILENCE`, replacing `--no-action-warnings`, refused for `mcp` and `lsp`); a silenced finding stays in the list, the JSON and the count, only the printed output leaves it out, and an error is never silenced | accepted |
| [0038](0038-an-entry-is-judged-by-where-it-stands-and-a-file-declares-no-kind.md) | an entry is judged by the position it stands in — the top level of a file, a node's body, a map of node names, a resource type map — by ONE check (`pipeline/position.rs`) that every walker calls and that a used file's entries get with the position of their `use`; a file declares no kind, the `content` header word is gone, a used file's statements reach the estate from every position, and a `suppress` in a used file is refused | accepted |
| [0039](0039-a-finding-is-laid-out-by-one-renderer-and-its-command-is-a-field.md) | one renderer (`findings::lay_out`) prints a finding for every command — a first line of severity, kind, `file:line` and subject, the message indented under it, `fix: <command>` last — and `Finding` gains `fix`, where the command MOVES out of `message`; a group's title carries no count, the prose is wrapped for a terminal and for nothing else, there is no colour, a front-end refusal is a refusal with one `front-end` finding, `main` prints an error by `Display`, and `transpile --format json` prints what `satz_transpile_check` returns | accepted |
| [0040](0040-findings-that-say-the-same-thing-share-one-block-and-the-producer-says-what-is-shared.md) | findings of one group whose severity, kind, `fix` and sentence are EQUAL print as one block — their first lines as a table, the sentence and `fix:` once — on a terminal and into a pipe; the sentence compared is one the PRODUCER states (`Finding::shared`, a notice's text without its pack and param), never one cut out of `message`; `shared` is in no JSON and no schema, a block of one prints its whole message, the counts are of findings, and the language server is not collapsed | accepted |
| [0041](0041-satz-carries-no-code-that-rewrites-an-estate-for-a-breaking-change.md) | a breaking change ships as a refusal naming the fix plus an entry under `## Breaking changes`, and satz carries no code that rewrites, repoints or migrates an estate for one — the moved-pack migration, the gating migration, the proof's allowance for bound gates and `GetPresetsReport.migrated` are deleted; the refusals (`MOVED_PACKS`, `RENAMED_PARAMS`), the `ungated-pack` finding, the standing update machinery (drift, auto-fork and repoint, `.diff.satz`, the transpile-identity proof, `--adopt`) and `adopt_pack_lines` stay, because they serve every change and not one past one; the hand edit is the operator's and is unproven by satz | accepted |
| [0042](0042-a-pack-declares-the-severity-of-what-it-says.md) | a pack declares the SEVERITY of what it says (`severity = error \| warning \| info` on a `notice`, replacing `before = apply`): an open `error` refuses every command that writes to the organisation, a `warning` is printed and the run goes on, an `info` waits for nothing; which commands write is one exhaustive table (`src/org_write.rs`) a new command cannot compile without joining, the gate runs once in front of the dispatch so no command names a notice, a run that only reads says an open error as a warning, `Severity::Note` becomes `Info` everywhere the word is published, and `review-pack` refuses an error-severity message whose text is no reason | accepted; supersedes the `before = apply` half of [0034](0034-a-pack-names-the-command-to-run-once-it-is-on.md) |
| [0043](0043-an-organisation-level-type-is-file-global-and-never-written-inside-a-project.md) | a type that belongs above a project — `google_folder`, `google_project`, and any type scoped `Org`, `Customer` or `Billing` — is written at the TOP LEVEL of a file, the file that declares a project included, and reaches its own scope from wherever that file is `use`d; written as a block directly inside a named project's body it is refused, naming what it belongs to and the edit. what a used file's entries are judged as is untouched (a used file is judged at its own top level), a folder's body is untouched, no resource moves in any plan, and an org policy in a project's body gets `parent = "projects/${…project_id}"` instead of the bare id no apply could take | accepted; [0038](0038-an-entry-is-judged-by-where-it-stands-and-a-file-declares-no-kind.md) applied to what a project's body takes |
| [0044](0044-a-tool-result-carries-the-worklist-not-the-table.md) | a tool result carries the worklist, not the table: `satz_adopt` returns the rows that ask for something — a decision, a state move, an import — at most fifty of them, with `rows_total`, `rows_omitted` and a `note` naming what is missing and where the rest is, and an `out` that writes the whole report as JSON under the root; `satz_report_compliance` stays whole, because an evidence report that omits evidence is a different document; the probe fails a result over the client's limit | accepted |
| [0045](0045-a-project-says-its-parent-and-an-empty-parent-says-nothing.md) | a pack that creates a project names the folder it is created in with a param of its own (`logsink_project_folder`, `mdc_mgmt_project_folder`), so the pack emits the same project wherever its line stands; a `folder_id`, `org_id` or `org` whose value is EMPTY is not stated and the enclosing node decides, exactly as if the key were absent, so no estate's plan moves; a value that is a dotted path is emitted as a reference to a node the estate declares, anything else as a literal id | accepted; the two packs [0046](0046-an-enclosure-holds-the-estates-own-resources-and-a-pack-is-used-at-the-top-level.md) would otherwise leave unable to place their project |
| [0046](0046-an-enclosure-holds-the-estates-own-resources-and-a-pack-is-used-at-the-top-level.md) | the body of a folder and the body of a project hold the estate's own resources, and a pack is used at the TOP LEVEL of a file: a `use` in a node's body is refused, naming the node, the edit and — for the two packs that create a project — the folder param to bind. 32 of the library's 34 packs emit byte-identical HCL nested or bare, and the two that did not now name their folder ([0045](0045-a-project-says-its-parent-and-an-empty-parent-says-nothing.md)). Nesting keeps its meaning for what an author writes directly and for an organisation-level type at a used file's top level; `google_folder { … }` and `use … as <type>` are untouched. The pack graph's placement collapses to the menu and a resource type map: `after_scaffold` is gone, `block` names a type and never a node | accepted; [0038](0038-an-entry-is-judged-by-where-it-stands-and-a-file-declares-no-kind.md) applied to where a `use` may stand |
| [0047](0047-a-resource-bodys-keys-are-the-schemas-and-nine-are-satzs-own.md) | every key of a resource body is checked against the provider schema in the FRONT END, level by level, and a key the schema does not name is a parse error naming the file, the line and the key; a key that opens a block is followed one level down, a key that carries a value is a leaf; nine keys are satz's own and are a closed list — `"import-id"`, `lifecycle`, `provider`, a project's `project_service` and `org`, a group's `member`, `manager`, `owner`, `email` — while `depends_on` is not one, and `single_resource_block`'s dead `google_project` branch is deleted | accepted |
| [0048](0048-satz-reads-no-yaml-estate.md) | satz reads no YAML estate or pack: the pre-Satz dialect's converter is deleted — `pre_pass`, `substitute_aliases`, `convert`, `inline_sequence_includes`, `retarget_uses`, `list_form`, `normalise_conditional_binding`, `convert_yaml_to_satz`, `--kind`/`--gate`/`--fork`, the corpus fixture and its gate — while the PRINTER every import writes through (`convert_value` and the helpers around it) stays untouched; a `.yaml` file is refused by name with the last release that converts it, written once in `satz_core::LAST_YAML_CONVERTING_RELEASE`, and `import-config.yaml` and the catalogs are data that stay YAML | accepted; reverses the "for as long as legacy orgs exist" clause of the 2026-08-29 rule |
| [0049](0049-fmt-takes-paths-and-resolves-no-estate-name.md) | `satz fmt` takes PATHS — files, directories, several at once — and resolves no estate name inside `yaml_dir` the way every other estate-consuming command does: it formats the whole tree, packs and library files included, and rewrites in place, so the file it touches is the one named. The refusal says what `fmt` takes and names the path that works when the name exists inside `yaml_dir` | accepted; the inconsistency is deliberate |
| [0050](0050-the-privacy-shapes-are-checked-twice-and-proven-equal.md) | the privacy gate's content rules are implemented twice — `scripts/check-names.sh` stays the gate, `src/privacy_shapes.rs` is what `review-pack` judges a pack with — and the two are held equal by a test that runs both over one fixture corpus (`tests/privacy-shapes/`) token by token; the allow-lists move out of the script into one file both read (`scripts/check-names-allow.txt`); a private-looking token in a pack is a `private-shape` error, and the directory-id and billing rules of the script judge tokens like every other rule | accepted |
| [0051](0051-a-pack-contributes-entries-to-another-packs-list-param.md) | a pack adds entries to another pack's list param by declaring `contributes_<param>` in its own `params` — the entries go after whatever the list holds, each once, they leave when the pack is switched off, and they are no variable of their own; an estate with no contributing pack emits byte-identical HCL. A well-known param name rather than a statement, so the pinned tree-sitter grammar, `satz fmt` and the fork-detecting canonical form all need nothing. A target no file declares drops the entries and leaves the need to the pack graph's `data` edge | accepted |
| [0052](0052-the-generate-config-fallback-is-a-flag-on-the-live-import-and-writes-a-second-file.md) | `satz import <live scope> --generate-unmapped` hands the resources the sweep could not map to the provider — one `import` block each in a scratch directory beside the estate, `tofu init`, `tofu plan -generate-config-out`, and the result back through the hcl import arm into a SECOND file, `<estate>-generated.satz`; the import id is the asset's relative resource name and the plan is what verifies it, only `unmapped` resources are generated for, and the flag is refused on the state and hcl shapes and with `--into` | accepted; the `--into` refusal is superseded by [0053](0053-the-generate-config-fallback-runs-under-into-as-well.md) |
| [0053](0053-the-generate-config-fallback-runs-under-into-as-well.md) | `--generate-unmapped` runs with `--into` too: the fallback's files are named after the run's packs (`imported-<scope>-generate/`, `imported-<scope>-generated.satz`), the `tofu` child reads as the estate's IaC service account through an `impersonate_service_account` in the provider block satz writes, and the delta's subtraction applies — an unmapped resource whose relative resource name IS a live id the estate resolved to is named and not generated for, by equality of the id and never by resemblance | accepted; supersedes the `--into` refusal of [0052](0052-the-generate-config-fallback-is-a-flag-on-the-live-import-and-writes-a-second-file.md) |
| [0054](0054-the-s1-model-ships-in-one-spelling.md) | the S1 security group model ships in ONE file, `security-group-models/s1-security-groups.satz`, used at the top level; the two-file spelling — `s1-group-definitions.satz` under `google_cloud_identity_group` and `s1-group-permissions.satz` under `google_organization_iam_member` — is deleted with its `offers` entries, its pages, its changelog rows and the `tests/iac/s1-split` case. The two emitted byte-identical HCL from the same params and asked the same five questions, so the choice between them carried nothing to act on. An estate holding the old lines is refused by name and line, and `presets/README.md`'s `## Breaking changes` carries the edit; with the only same-gate pair gone, two packs that exclude one another and both deploy are always an error | accepted |
| [0055](0055-satz-writes-the-mcp-client-configuration.md) | `satz mcp-config <estate>` prints the block an MCP client reads to start `satz mcp` — the running binary by absolute path, the estate's own directory as `--root`, and the ceiling ALWAYS written out, default included; two shapes (`.mcp.json` under the key `satz`, Claude Desktop's entry under `satz-<estate>`), and `--write` MERGES satz's one key into the client's file, leaving every other server as it is, refusing a satz key that differs until `--force` and never rewriting a file it cannot parse | accepted |
| [0056](0056-a-live-import-flattens-nested-values-onto-the-attributes-the-provider-names.md) | a live import flattens what the API nests onto the attribute the provider names — a leaf whose name is an attribute of the block, or the `enabled`/`value` of an object of that name — reading the data against the provider schema, so it needs no generated `type-map.yaml`; a fact the API states in other terms is a written-out pair (`isLive` → `with_state`); two fields claiming one attribute with different values carry neither and both are named; an attribute the schema names whose value could not be placed is reported per resource on every run, apart from the API vocabulary the provider does not speak; `force_destroy` is readable from nothing and is documented as never imported | accepted |
| [0057](0057-a-live-import-is-told-which-estate-to-read-as.md) | a live `satz import` takes its identity from an estate the operator NAMES: `--as <estate>` binds that estate's IaC service account and writes a new file, `--into` binds the same way and writes the delta, the two are refused together, and given neither the sweep reads as the caller's own credentials — which `IDENTITIES` now says as `HumanOrEstate`. Deriving the estate from `--config` or from which estate names the organisation was rejected as a guess about live identity. A refused sweep names the credential it ran as and that `roles/cloudasset.viewer` is the service account's | accepted |
| [0058](0058-the-asset-name-picks-the-terraform-type.md) | which import-config row a live asset is mapped through is decided by the asset: its content type, then the parent read off its own name (`projects/…`, `folders/…`, `organizations/…`, `billingAccounts/…`) against the parent each candidate Terraform type is for. Several rows still fitting is reported as `ambiguous` with the types named and `--only` as the lever, never resolved by the table's order — which had `--all` map every log sink and log bucket of a live organisation to the billing-account type and then drop them. A `parent:` column in the 841-row table and a per-asset-type default row were both rejected | accepted |
| [0059](0059-a-projects-provider-alias-is-the-estates-provider-scoped-to-that-project.md) | the per-project provider alias a `google_project` gets — the one every resource inside that project's body is served by — is the estate's `google` provider scoped to that project: `billing_project` and `user_project_override` make the project its OWN quota project, so Google tests API enablement where the resource lives instead of on the infrastructure project (a 403 on a live key ring whose project had the API on), and `region` comes from the estate's `google` provider block instead of the `"europe-west3"` literal the emitter carried, an estate that names no region getting aliases that name none. The estate's own provider blocks and the central quota project they declare are untouched, and the prerequisite check still judges the infrastructure project | accepted |
| [0060](0060-a-sweep-isolates-the-asset-type-the-api-refuses.md) | a live sweep that Cloud Asset Inventory refuses for one asset type in the request asks the request again in halves until the refusal is down to a single type, leaves that type out and ends the run by naming it with the rows that asked for it and what the API said; a denied scope, a broken connection or any other error still ends the run at the first answer. A stale entry in the derived `presets/cai-asset-types.txt` used to abort a whole `--all` import with nothing written | accepted; narrows the fail-fast rule of the live sweep |
| [0061](0061-satz-writes-no-deletion-default-and-a-teardown-is-an-estate-edit.md) | satz emits no `deletion_policy` for a project and no `deletion_protection` for a folder unless the estate declares one, so the provider's own protection stands and `tofu destroy` is refused until the estate says otherwise; the teardown — two estate edits, two targeted applies, then the destroy — is documented in `docs/workflows.md`. Emitting `DELETE`/`false` by default, emitting the protective values explicitly, a `satz destroy` command and an `allow_destroy` param were all rejected | accepted |
| [0062](0062-the-hcl-import-carries-the-provider-drops-the-ordering-and-refuses-a-reference-that-crosses-the-boundary.md) | the hcl import judges each meta-argument by whether Satz can say it: `provider` is a Satz body key and travels with the resource (the estate's own alias left to the emitter, a form with no room for one wrapping instead), `depends_on` is DROPPED per edge and reported, because satz derives a plan's ordering from the estate — and only where every address it names is a block the import carries. A `${…}` from a translated resource to a wrapped one, or to an address no block declares, REFUSES the import naming both sides, where it used to warn and write an estate `satz transpile` refuses; a label is letters, digits and `_`, and a grant map's member must be one. Wrapping the referring block instead of refusing, and a tenth Satz body key for `depends_on`, were both rejected. Measured on satz's own 90-resource emission: 0 translated before, 83 after, same addresses | accepted |
| [0063](0063-an-import-refuses-rather-than-write-a-value-the-source-does-not-carry.md) | `satz import` writes an estate only when it can write every value the estate is made of: the organisation comes from a resource that names it or from a top-level folder's parent (which the nesting pass now returns instead of dropping), `--organization <n>` supplies it on the state shape where the source names none, and a source that names none without the flag refuses the write with nothing written — where it used to warn and write `parent = "organizations/"` and `org_id = ""`. A grant whose scope the source does not carry is refused the same way. Warning and writing, an empty param left to `satz transpile`, an interactive question, the import config's `root:` and a flag on every shape were all rejected | accepted; the fail-fast rule applied to the import's own values |
| [0064](0064-an-empty-value-the-provider-schema-names-is-a-value.md) | an import keeps an empty STRING on an attribute the provider schema names — `google_pubsub_subscription.expiration_policy.ttl = ""` is never expires, and dropping it restored Google's 31-day default — while null, `[]`, `{}` and an empty value on a key the schema does not name are still dropped; where the API states the same fact by leaving a block empty, the pair is a row in `translate_api_values`. Keeping every empty value, a per-type exception list, reading the schema's prose description and deriving `""` for any required string attribute of an empty block were all rejected | accepted |
